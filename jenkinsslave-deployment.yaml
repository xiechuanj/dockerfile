apiVersion: extensions/v1beta1  
kind: Deployment  
metadata:  
  name: ibis2
spec:  
  replicas: 1  
  strategy:  
    type: RollingUpdate  
    rollingUpdate:  
      maxSurge: 2  
      maxUnavailable: 0  
  template:  
    metadata:  
      labels:  
        app: ibis2
    spec:  
      containers:  
      - name: ibis2 
        image: xiechuan/centos721511jenkinsslave:1.0.0
        imagePullPolicy: IfNotPresent  
        env:
          - name: jenkinsParas
            value: "{'jenkinsUrl':'http://jenkins:8080','username':'admin','password':'admin123','nodename':'ibis2'}"
        ports:  
        - containerPort: 8080  
          name: tomcat 
          protocol: TCP  

